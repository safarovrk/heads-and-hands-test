class Monster(
    attack: Int,
    defence: Int,
    healthPoints: Int,
    minDamage: Int,
    maxDamage: Int
) : Creature(
    attack,
    defence,
    healthPoints,
    minDamage,
    maxDamage
)
